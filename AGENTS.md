# LiftLog Hard Copy

This repository is a short-lived static prototype for LiftLog workout backups.
Keep both prototype sets (`analysis/` and `classic/`) intact and keep the mobile smoke tests passing.
